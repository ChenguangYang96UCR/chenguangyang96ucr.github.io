import React from "react";
import ReactDOM from "react-dom/client";
import App from "./graph_node_inspector_template.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);